#include <stdio.h>
#include <math.h>
int main(){

  //Modular Operator " % "
  //This operator gives remainder

  int a, b;
  printf("Enter the divisor \n");
  scanf("%d", &a);

  printf("Enter the dividend \n");
  scanf("%d", &b);

  printf("The remainder is %d \n", b%a);
  printf("using exponential function pow %f \n", pow(2, 5));

  return 0;

}