#include <stdio.h>
int main (){
    
    /* A do-while loop is executed:

       At least once
       At least twice
       At most once

    */

   printf("The correct answer is (a) at least one !!\n");

   //Q. What can be done using one type of loop can also 
   //be done using the other two types of loops – True or False.

   printf("True\n");
    
    return 0;
}