#include <iostream>
using namespace std;
int main (){
    
    //Using Do while loop;
    //and the basic cruz is it executes at least once

    int index = 0;

    do{
        cout<<"We are at index: "<<index<<endl;
        
        index++;
    }
    while(index<=10);

    // here in above example it executes at least once whereas the condition is false



    
    return 0;
}